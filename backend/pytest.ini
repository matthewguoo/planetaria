; Async tests run without per-test markers. Before this file existed every
; async test carried a hand-written @pytest.mark.asyncio, and a forgotten
; marker was a test that silently skipped while reporting green.
[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
