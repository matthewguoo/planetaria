# Correctness-only lint: F (pyflakes) catches undefined names, unused
# imports, and redefinitions; E9 catches syntax/indentation errors.
# Run: ruff check .
target-version = "py313"

[lint]
select = ["F", "E9"]
